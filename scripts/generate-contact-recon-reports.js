#!/usr/bin/env node
const fs=require('fs'),path=require('path'),Database=require('better-sqlite3');const {ensureTables}=require('../lib/db');ensureTables();
const root=path.resolve(__dirname,'..'),out=path.join(root,'contact-recon-reports');fs.mkdirSync(out,{recursive:true});const db=new Database(path.join(root,'data','crm.db'));
const jobs=db.prepare(`SELECT j.*,c.description,c.products,c.industry,c.customer_type,s.business_summary,s.likely_component_needs_json,s.match_score,s.match_group,s.risk_level,s.risk_reasons_json FROM contact_recon_jobs j JOIN customer_pool c ON c.customer_id=j.customer_id LEFT JOIN company_screening s ON s.customer_id=j.customer_id WHERE j.status='done' AND j.result_json!='' ORDER BY j.updated_at`).all();
const esc=v=>String(v??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));const arr=v=>{try{return JSON.parse(v||'[]')}catch{return[]}};const safeUrl=v=>/^https?:\/\//i.test(String(v||''))?String(v):'';
function render(job){let result={};try{result=JSON.parse(job.result_json)}catch{};const people=db.prepare(`SELECT * FROM person_candidates WHERE contact_recon_job_id=?`).all(job.job_id).map(p=>({...p,sales_ready:Boolean(p.sales_ready),employment:{status:p.employment_status,confidence:p.employment_confidence},methods:db.prepare('SELECT method_type type,value,discovery_type,verification_status,is_direct,is_generic,is_inferred,source_url FROM contact_methods WHERE person_id=?').all(p.person_id).map(m=>({...m,is_direct:Boolean(m.is_direct),is_generic:Boolean(m.is_generic),is_inferred:Boolean(m.is_inferred)}))}));const evidence=db.prepare('SELECT * FROM person_evidence WHERE contact_recon_job_id=?').all(job.job_id);const rank={P3:3,P2:2,P1:1,P0:0};people.sort((a,b)=>(b.sales_ready?10:0)+(rank[b.procurement_relevance]||0)-(a.sales_ready?10:0)-(rank[a.procurement_relevance]||0));const ready=people.filter(x=>x.sales_ready),verifiedEntry=people.filter(x=>x.contact_level==='L3'&&!x.sales_ready);const verdict=ready.length?`可交付：${ready.length}名采购/技术相关直联人`:(verifiedEntry.length?`可触达：${verifiedEntry.length}名真实入口人，需转采购`:'未找到可直接交付的负责人');const needs=arr(job.likely_component_needs_json);
const personHtml=people.map(p=>{const methods=(p.methods||[]).filter(m=>m.value).map(m=>`<span class="method"><b>${esc(m.type)}</b> ${esc(m.value)}${m.is_inferred?' <em>推断</em>':''}</span>`).join('');const sources=evidence.filter(e=>e.person_id===p.person_id&&e.source_url).slice(0,4).map(e=>`<a href="${esc(e.source_url)}" target="_blank" rel="noopener">${esc(e.source_title||e.evidence_type||'证据')}</a>`).join('');return`<article class="person ${p.sales_ready?'ready':''}"><div class="person-head"><div><h3>${esc(p.full_name_local||p.full_name||'未知')}</h3><p>${esc(p.title||'职位未确认')}</p></div><div><span class="level">${esc(p.contact_level||'L0')}</span><span class="level relevance">${esc(p.procurement_relevance||'P0')}</span></div></div><div class="methods">${methods||'<span class="muted">无直接联系方式</span>'}</div><p class="status">任职：${esc(p.employment?.status||'unverified')} · 交付：${esc(p.delivery_status||'research_only')}</p><div class="sources">${sources}</div></article>`}).join('');
const gapHtml=(result.search_gaps||[]).slice(0,8).map(x=>`<li>${esc(x)}</li>`).join(''),nextHtml=(result.recommended_next_searches||[]).slice(0,6).map(x=>`<li>${esc(x)}</li>`).join('');const website=safeUrl(job.website)?job.website:`https://${job.website||''}`;
return`<!doctype html><html lang="zh-CN"><head><meta charset="utf-8"><meta name="viewport" content="width=device-width,initial-scale=1"><title>${esc(job.company_name)} - 销售联系人报告</title><style>:root{--ink:#17212b;--muted:#667085;--brand:#087f70;--bg:#eef3f7;--line:#d9e2e8;--ok:#e8f7f1;--warn:#fff5dc}*{box-sizing:border-box}body{margin:0;background:var(--bg);font:15px/1.55 -apple-system,BlinkMacSystemFont,"Segoe UI","PingFang SC",sans-serif;color:var(--ink)}main{max-width:1080px;margin:28px auto;padding:0 18px 48px}.hero,.card,.person{background:#fff;border:1px solid var(--line);border-radius:16px;box-shadow:0 8px 24px rgba(20,48,65,.06)}.hero{padding:28px}.eyebrow{font-size:12px;letter-spacing:.12em;color:var(--brand);font-weight:700}.hero h1{margin:6px 0 8px;font-size:32px}.hero p{margin:4px 0;color:var(--muted)}.verdict{margin-top:20px;padding:15px 18px;border-radius:12px;background:${ready.length?'var(--ok)':'var(--warn)'};font-size:18px;font-weight:700}.grid{display:grid;grid-template-columns:1.1fr .9fr;gap:16px;margin-top:16px}.card{padding:22px}.card h2{margin:0 0 12px;font-size:19px}.tags{display:flex;flex-wrap:wrap;gap:8px}.tag,.level{display:inline-block;padding:4px 9px;border-radius:999px;background:#e9f4f2;color:#08695d;font-size:12px;font-weight:700}.level{background:#edf2f7;color:#344054}.relevance{background:#e8f7f1;color:#087f70;margin-left:5px}.people{display:grid;gap:12px}.person{padding:18px;box-shadow:none}.person.ready{border:2px solid #57b79f}.person-head{display:flex;justify-content:space-between;gap:12px}.person h3{margin:0;font-size:18px}.person p{margin:2px 0;color:var(--muted)}.methods{display:flex;flex-wrap:wrap;gap:8px;margin:13px 0}.method{background:#f4f7f9;border-radius:8px;padding:7px 10px}.method em{color:#b54708}.sources{display:flex;flex-wrap:wrap;gap:10px}.sources a,a{color:#087f70}.status{font-size:13px}.muted{color:var(--muted)}ul{margin:8px 0;padding-left:20px}.foot{margin-top:16px;font-size:12px;color:var(--muted)}@media(max-width:760px){.grid{grid-template-columns:1fr}.hero h1{font-size:25px}}</style></head><body><main><header class="hero"><div class="eyebrow">CONTACT RECON · 销售简版</div><h1>${esc(job.company_name)}</h1><p>${esc(job.customer_id)} · ${esc(job.industry||job.customer_type||'')} · 匹配 ${esc(job.match_score||0)}/100 · 风险 ${esc(job.risk_level||'unknown')}</p><p><a href="${esc(website)}" target="_blank" rel="noopener">${esc(job.website||'未找到官网')}</a></p><div class="verdict">${esc(verdict)}</div></header><section class="grid"><div class="card"><h2>公司做什么</h2><p>${esc(job.business_summary||job.description||'信息不足')}</p></div><div class="card"><h2>可能需求</h2><div class="tags">${needs.map(x=>`<span class="tag">${esc(x)}</span>`).join('')||'<span class="muted">待判断</span>'}</div></div></section><section class="card" style="margin-top:16px"><h2>人员与联系方式</h2><div class="people">${personHtml||'<p class="muted">未找到具名人员</p>'}</div></section><section class="grid"><div class="card"><h2>当前缺口</h2><ul>${gapHtml||'<li>无额外缺口记录</li>'}</ul></div><div class="card"><h2>建议下一步</h2><ul>${nextHtml||'<li>根据人员等级执行人工复核</li>'}</ul></div></section><p class="foot">生成时间 ${esc(job.updated_at)} · L3=联系方式真实度，P3/P2=采购或技术选型相关性。交易前请执行制裁、物项与最终用户合规复核。</p></main></body></html>`}
const update=db.prepare('UPDATE contact_recon_jobs SET report_path=? WHERE job_id=?');for(const job of jobs){const file=path.join(out,`${job.job_id}.html`);fs.writeFileSync(file,render(job));update.run(file,job.job_id)}db.close();console.log(JSON.stringify({generated:jobs.length,output:out},null,2));
