# TradePulse Planning

`docs/planning/` 不再承载当前路线图或执行台账。2026-07-25 的两份旧计划已经冻结并移入 `docs/archive/planning-2026-07-25/`，只保留为审计证据。

当前进度、基线和下一步必须按以下顺序核验：实时远端 `main`、生产 `current`/release state、`after/` 的 Git/代码/测试，以及 `docs/governance/`。归档内容不得作为当前任务输入。
